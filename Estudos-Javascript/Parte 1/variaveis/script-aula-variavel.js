// Declarar uma variável com o seu nome
var nome = "Renan";
// Declarar uma variável com a sua idade
var idade = 27;
// Declarar uma variável com a sua comida
// favorita e não atribuir valor
var comidaFavorita;

// Atribuir valor a sua comida favorita
comidaFavorita = "Churrasco";

// Declarar 5 variáveis diferentes sem valores

var sobreNome, sexo, cidade, estado, cep;

console.log(nome,idade, comidaFavorita);

// Ao declarar uma variável, ela é carregada para o início do código, porém sem o seu valor atribuido
// No exemplo abaixo, irá reconhecer a variavel, porém sem o seu valor //Undefined
console.log(comida);
var comida = 'Pizza';

// O correto é declarar a variavel e seu valor antes dela ser usada.

var comida = 'Pizza';
console.log(comida);

// É possível alterar o valor atribuído a uma variável.

var time = 'Corinthians';
console.log(time);

// Basta alterar como no exemplo abaixo.

time = 'Flamengo';
console.log(time);
